// The Logfire-backed run-history browser, now at /history (home redirects to the live /live view).
// Re-exports the browser defined in app/page.tsx so that file stays the single source of its components.
export { default } from "../page";
