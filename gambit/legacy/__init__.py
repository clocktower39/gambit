"""Legacy pre-typed negotiation stack.

These modules preserve the original dataclass-based demo and DO-inference path while
the active implementation lives in `gambit.engine` and `gambit.negotiation`.
"""
